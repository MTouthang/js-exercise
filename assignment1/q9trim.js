/**
 * 9. Use trim() to remove any trailing whitespace at the beginning and the end of a string.
 */

const str =
  "   JavaScript is a prototype-base, multi-paradigm, single-threaded, dynamic language  ";
const trimStr = str.trim();

console.log(trimStr);
