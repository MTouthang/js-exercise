/**
 * Calculate the slope, x-intercept and y-intercept of y = 2x -2
 */
let x1 = 0;
let y1 = 0;
let y2 = 2 * 0 - 2;
let x2 = (y2 + 2) / 2;

let m = (y2 - y1) / (x2 - x1);

console.log("slope", m);
